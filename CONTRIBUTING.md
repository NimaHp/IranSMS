# راهنمای مشارکت (Contributing)

**فارسی** | [English](CONTRIBUTING.en.md)

از تمایل شما برای مشارکت در پروژهٔ IranSMS سپاس‌گزاریم! این راهنما مسیر مشارکت و ارسال تغییرات را برای شما شفاف و آسان می‌کند.

## پیش‌نیازها

* .NET 10 SDK

## راه‌اندازی پروژه

مخزن را دریافت کرده و پروژه را بیلد کنید:

```bash
git clone <repository-url>
dotnet restore IranSMS.slnx
dotnet build IranSMS.slnx -c Release
```

## اجرای تست‌ها

برای اجرای مجموعه تست‌های واحد:

```bash
./tests/IranSMS.Tests/bin/Release/net10.0/IranSMS.Tests.exe
# یا
dotnet build IranSMS.slnx -c Release
```

* تمامی تست‌ها باید با موفقیت پاس شوند (۱۵۰ تست با `xunit v3` + `FluentAssertions`).
* ارائه‌دهندهٔ `Mock` با شناسه‌های قطعی `mock-{n}` برای آزمون محلی در دسترس است.

## قواعد و اصول کدنویسی

* **مدیریت هشدارها:** تمامی پروژه‌ها از `.editorconfig` و تنظیم `TreatWarningsAsErrors` پیروی می‌کنند؛ هیچ هشداری (Warning) در بیلد پذیرفته نمی‌شود.
* **ویژگی‌های .NET:** قابلیت‌های `Nullable` و `ImplicitUsings` در سراسر پروژه فعال هستند؛ `LangVersion` روی `latest` است.
* **سازگاری `netstandard2.0`:** هسته و همهٔ ارائه‌دهندگان `netstandard2.0` هستند — از APIهای اختصاصیِ `net8`/`net10` در `src/` استفاده نکنید؛ در تست‌ها و نمونه‌ها مجاز است.
* **مالکیت `HttpClient`:** چرخهٔ عمر `HttpClient` در اختیار مصرف‌کننده است؛ در `src/` هیچ `HttpClient` سراسری یا `IHttpClientFactory` نسازید — نمونه از بیرون تزریق می‌شود.
* **ثبت قابلیت‌محور:** ثبت در `IranSms.DependencyInjection` باید قابلیت‌محور باشد؛ تنها اینترفیس‌هایی ثبت شوند که نمونه واقعاً پیاده‌سازی کرده است — از `cast` کور خودداری کنید.
* **عدم جعل شناسه:** در صورت نبودِ `MessageId` از سمت اپراتور، شناسهٔ ساختگی تولید نکنید؛ `IranSmsException` با `RawResponseBody` پرتاب کنید.
* **کدهای خطا:** خطاهای اپراتور باید به `IranSmsException` با `ProviderName` و `ProviderStatusCode` نگاشت شوند؛ `RawResponseBody` فقط برای عیب‌یابیِ امن است.
* **حدود ورودی:** محدودیت‌های هر اپراتور (سقف گیرندگان انبوه، طول پیام، الزام `senderLine`) باید پیش از ارسال شبکه بررسی شود.
* **بدون وابستگی اضافی:** `Core` بدون وابستگی بماند؛ هر ارائه‌دهنده تنها به `Core` وابسته باشد.

## مستندات دوزبانه

زبان اصلی مستندات، فارسی است (فایل‌های بدون پسوند). مستندات انگلیسی با پسوند `*.en.md` مشخص می‌شوند.

* هرگونه تغییر در مستندات باید به‌صورت هم‌زمان در **هر دو زبان** اعمال شود.
* فایل‌های انگلیسی نباید حذف شوند.
* نوار تغییر زبان در بالای تمامی فایل‌های مستندات باید حفظ شود.

## چک‌لیست ثبت Pull Request

پیش از ارسال PR، موارد زیر را بررسی و تأیید کنید:

* [ ] بیلد و تست‌ها بدون هیچ هشداری پاس می‌شوند.
* [ ] تغییرات در بخش [Unreleased] فایل `CHANGELOG.md` (در هر دو زبان) ثبت شده است.
* [ ] مستندات دوزبانه در صورت نیاز به‌روزرسانی شده‌اند.
* [ ] برای تغییرات حساس به `HttpClient` یا نگاشت خطا، تست با `FakeTransport` افزوده شده است.
